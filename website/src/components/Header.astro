---
const base = import.meta.env.BASE_URL;
---

<header>
  <div class="header-inner">
    <a class="site-title" href={base}>【原神】星々の幻境 自己流制作マニュアル</a>

    <nav>
      <a href={`${base}about`}>このサイトについて</a>
      <a href={`${base}about_miliastra`}>星々の幻境とは</a>
      <a href={`${base}beginner`}>これから制作する方へ</a>
<!--       <a href={`${base}features`}>機能説明</a> -->
      <div class="header-search">
        <div id="header-search"></div>
      </div>

      <link
        rel="stylesheet"
        href="/genshin-miliastra-guide/pagefind/pagefind-ui.css"
      />

      <script
        is:inline
        src="/genshin-miliastra-guide/pagefind/pagefind-ui.js"
      ></script>

      <script is:inline>
        window.addEventListener("DOMContentLoaded", () => {
          new PagefindUI({
            element: "#header-search",
            showSubResults: true,
            showImages: false,
          });
        });
      </script>
    </nav>
  </div>
</header>

<style>
  header {
    border-bottom: 1px solid #ddd;
    padding: 16px 24px;
  }

  .header-inner {
    max-width: 960px;
    margin: 0 auto;
  }

  .header-search {
    width: 240px;
  }

  /* 検索フォーム */
  .header-search .pagefind-ui__search-input {
    height: 40px;
    font-size: 14px;
    padding-left: 2.4rem;
  }

  /* 虫眼鏡アイコン */
  .header-search .pagefind-ui__search-clear,
  .header-search .pagefind-ui__search-icon {
    transform: scale(0.8);
  }

  /* 検索ボックス全体 */
  .header-search .pagefind-ui__form {
    margin: 0;
  }

  /* 結果ウィンドウ */
  .header-search .pagefind-ui__drawer {
    position: absolute;
    width: 420px;
    right: 0;
    z-index: 1000;
  }

  .site-title {
    display: inline-block;
    font-weight: bold;
    font-size: 20px;
    margin-bottom: 12px;
    text-decoration: none;
    color: inherit;
  }

  nav {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
  }

  nav a {
    color: inherit;
    text-decoration: none;
  }

  nav a:hover {
    text-decoration: underline;
  }
</style>